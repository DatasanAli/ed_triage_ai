# ------------------------------------------------------------
# Root requirements — installed in SageMaker Studio JupyterLab
# Packages already in the conda base env are NOT listed here
# to avoid version-conflict backtracking:
#   boto3, langchain-aws, langchain, langchain-openai,
#   torch, transformers, scikit-learn, lightgbm, joblib,
#   numpy, scipy, pydantic
# ------------------------------------------------------------

# RAG & LLM — agent graph dependencies
langgraph>=1.1.0
langchain-core>=0.3.49,<1.0.0
langchain-anthropic>=0.3.0
anthropic>=0.40.0

# Vector database & embeddings
pinecone>=3.0.0

# Configuration & utilities
python-dotenv>=1.0.0
pandas>=2.2.0
